<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="channel">

    <select id="getChannelList" resultType="glico.pocky.domain.channel.domain.Channel">
        SELECT
            CHANNEL_ID,
            CHANNEL_TITLE,
            CHANNEL_SLUG,
            IS_DELETED,
            DATE_FORMAT(CREATED_AT, '%Y-%m-%d %H:%i:%s') AS CREATED_AT,
            DATE_FORMAT(UPDATED_AT, '%Y-%m-%d %H:%i:%s') AS UPDATED_AT
        FROM GLICO.CHANNEL
    </select>

    <select id="getChannelById" parameterType="String" resultType="glico.pocky.domain.channel.domain.Channel">
        SELECT
            CHANNEL_ID,
            CHANNEL_TITLE,
            CHANNEL_SLUG,
            IS_DELETED,
            DATE_FORMAT(CREATED_AT, '%Y-%m-%d %H:%i:%s') AS CREATED_AT,
            DATE_FORMAT(UPDATED_AT, '%Y-%m-%d %H:%i:%s') AS UPDATED_AT
        FROM GLICO.CHANNEL
        WHERE CHANNEL_SLUG = #{channelSlug}
    </select>

</mapper>