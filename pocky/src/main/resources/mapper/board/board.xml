<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

    <select id="getBoardList" resultType="glico.pocky.domain.board.domain.Board">
        /* board.getBoardList */
        SELECT B.BOARD_ID ,
                B.TITLE,
                B.CONTENT ,
                B.CREATED_AT,
                M.USERID ,
                C.CHANNEL_TITLE
        FROM BOARD B /* 게시글 */
                 LEFT JOIN GLICO.MEMBER M ON M.ID = B.AUTHOR
                 LEFT JOIN GLICO.CHANNEL C ON C.CHANNEL_ID = B.CHANNEL
    </select>

    <insert id="boardInsert" >

    </insert>

    <update id="boardUpdate">


    </update>

</mapper>