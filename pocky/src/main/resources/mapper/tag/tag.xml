<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tag">

    <select id="tagList" resultType="glico.pocky.domain.tag.domain.Tag">
        SELECT *
        FROM GLICO.TAG
    </select>


    <insert id="tagInsert" parameterType="glico.pocky.domain.tag.domain.Tag"></insert>
</mapper>